[phases.setup]
nixpkgs = ["gcc", "gnumake"]

[phases.build]
cmds = [
    "echo Compilando motor C++...",
    "g++ motor-cpp/src/*.cpp -std=c++17 -O3 -pthread -o motor_adn"
]

[start]
cmd = "uvicorn app.main:app --host=0.0.0.0 --port=${PORT}"
